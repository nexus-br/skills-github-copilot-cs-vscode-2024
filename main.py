import os
import Base64
class Text(BaseModel1):
  Text1: str
# Create a FastAPI endpoint that accepts a POST request with a JSON body containing a single field called "text" and returns a checksum of the text1
